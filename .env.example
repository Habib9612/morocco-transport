# Morocco Transport Platform - Environment Variables
# Copy this file to .env.local and update the values for your local development

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# PostgreSQL Database (matches docker-compose.yml defaults)
DB_USER=morocco_transport
DB_PASSWORD=morocco_password
DB_NAME=morocco_transport
DB_HOST=localhost
DB_PORT=5432
# Database URLs for Prisma
DATABASE_URL="postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}"
DIRECT_URL="postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}"

# =============================================================================
# REDIS CONFIGURATION
# =============================================================================

# Redis for caching and sessions
REDIS_PASSWORD=redis_password
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_URL="redis://:${REDIS_PASSWORD}@${REDIS_HOST}:${REDIS_PORT}"

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================

# Environment
NODE_ENV=development

# NextAuth.js Configuration
NEXTAUTH_SECRET="your-nextauth-secret-key-here-change-this-in-production"
NEXTAUTH_URL="http://localhost:3000"

# =============================================================================
# DEPLOYMENT CONFIGURATION
# =============================================================================

# For production deployment, update these values:
# - Use strong, unique passwords
# - Use production database URLs
# - Set NODE_ENV=production
# - Generate secure NEXTAUTH_SECRET

# Example production DATABASE_URL:
# DATABASE_URL="postgresql://username:password@your-db-host:5432/database_name"

# =============================================================================
# DEVELOPMENT NOTES
# =============================================================================

# 1. Copy this file to .env.local for local development
# 2. Never commit .env.local or any .env files to version control
# 3. Update passwords and secrets for production deployment
# 4. Use docker-compose up -d to start PostgreSQL and Redis locally
# 5. Run 'npx prisma generate' and 'npx prisma db push' after setting up database
